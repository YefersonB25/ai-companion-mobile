/**
 * Mobile Auth Store
 *
 * Re-exports from @aria/core — shared across web and mobile.
 * Initialization happens in app/_layout.tsx with mobile-specific config.
 */

export { useAuthStore, initializeAuthStore } from '@aria/core'
